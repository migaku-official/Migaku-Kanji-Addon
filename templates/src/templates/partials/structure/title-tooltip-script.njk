{# note corresponding markup macro in `title-tooltip-macro.njk` #}
<script>
	function toggleShowTooltip(targetId, state) {
		var tooltipContainer = document.getElementById(targetId);

		function open() {
			tooltipContainer.style.display = 'block';
			tooltipContainer.setAttribute('aria-expanded', 'true');
		}

		function close() {
			tooltipContainer.style.display = 'none';
			tooltipContainer.setAttribute('aria-expanded', 'false');
		}

		if (typeof state === 'boolean') {
			if (state) {
				open();
			} else {
				close();
			}
		} else {
			if (tooltipContainer.getAttribute('aria-expanded') === 'false') {
				open();
			} else {
				close();
			}
		}
	}

	function toggleShowAllTooltips(state) {
		var allTooltips = document.querySelectorAll('.title-tooltip__text');

		for (var i = 0; i < allTooltips.length; i += 1) {
			var id = allTooltips[i].getAttribute('id');
			toggleShowTooltip(id, state);
		}
	}
</script>
